---
title: Badge
description: Displays a badge or a component that looks like a badge.
---

Standard styled badges with variance styles.

<ComponentPreview name="badge" />

---

## Installation

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npx @gravcn/cli add badge
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm dlx @gravcn/cli add badge
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn dlx @gravcn/cli add badge
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bunx @gravcn/cli add badge
    ```
  </Tab>
</Tabs>


---

## Usage Example

```tsx title="demo/Badge.tsx"
import { Badge } from "@/components/ui/badge";

export default function BadgeDemo() {
  return (
    <div className="flex gap-2">
      <Badge>Default</Badge>
      <Badge variant="secondary">Secondary</Badge>
      <Badge variant="destructive">Destructive</Badge>
      <Badge variant="outline">Outline</Badge>
    </div>
  );
}
```
