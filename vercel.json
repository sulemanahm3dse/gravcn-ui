{
  "buildCommand": "pnpm --filter registry build && pnpm --filter cli build && pnpm --filter www build",
  "outputDirectory": "apps/www/.next",
  "installCommand": "pnpm install",
  "devCommand": "pnpm --filter www dev",
  "build": {
    "env": {
      "ENABLE_EXPERIMENTAL_COREPACK": "1"
    }
  }
}